<UserControl x:Class="Moodify.View.MyPlaylists"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Moodify.View"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800">
	<!--Title="MyPlaylists" Height="450" Width="800">-->
    
    <StackPanel Background="LightBlue">
        <Label HorizontalAlignment="Left" Content="My Playlists" FontSize="25" FontWeight="Bold" Foreground="Blue" Padding="10"/>
        <ListBox ItemsSource="{Binding VM_PlaylistsDic}" ScrollViewer.CanContentScroll="True" Width="Auto" Height="Auto">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="10,10,10,10">
                        <TextBlock Text="{Binding Path=Key}" Margin="10, 10, 10, 10"/>
                        <Button Content="{Binding Path=Value.PlaylistName}" CommandParameter="{Binding Path=Key}" Click="OpenPlaylist_Click" >
                        </Button>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </StackPanel>
</UserControl>
